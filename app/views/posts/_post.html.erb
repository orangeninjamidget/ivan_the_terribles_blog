<section class="post">
  <h2>Post <%= "#{post.id}: #{post.title}" %></h2>
  <div class="post"><%= post.body %></div>
  <%= render 'post_nav', post: post %>
  <%= post.comments.size if post.comments.size > 0 %> Comments
  <%= render post.comments %>
</section>
